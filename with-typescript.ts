// union type can be add many type as possible
let course: string | number = 'React - The complete Guide'

course = 12341

// type Aliases 

type Person = {
    name: string,
    age: number
}

const person: Person = {
    name: 'thao',
    age: 26
}